# 方案：广告异常监控板块

> **状态：待确认，未实施。**
> 加板块属于 CLAUDE.md 里「结构改动必须先讨论」的范畴，所以先出方案。
> 缺口的事实描述见 [ARCHITECTURE.md §4.4](./ARCHITECTURE.md)。

---

## 要解决的问题

产品目标第三条：「一到公司打开 OA 就知道哪些产品要补货、**哪些产品的广告要调整**」。

补货那半边已经跑通。广告这半边目前在网页上不存在 —— 领星导出和分析脚本
可能已在本机存在某种形态，但没有正式接进 OA。

---

## 决策 1：上游产出 JSON，不要 HTML

**这是本方案最重要的一条，也是唯一一条我建议不要妥协的。**

补货板块所有的痛都来自同一个根因：上游只产 HTML，没有配套 JSON，
于是 OA 侧只能靠正则去解析 HTML。代价已经付过三次：

| 事故 | 出处 |
|------|------|
| `oa/restock.py` 整个模块就是一个容错的 HTML 解析器，上游改一个标签就可能失效 | D10 |
| 脱敏的 KPI 正则 `(.*?)` 跨了卡片边界，把 46 个详情页的「售价」「库存状态」两张卡整个吞掉 —— 而只查「敏感数据还在不在」完全看不出来 | D10 |
| 列表页 `<header class="hero">` 末尾多一个 `</div>`，单页打开看不出问题，一嵌进容器就把容器提前闭掉 | 本轮做预览页时踩到 |
| 16 个详情页公开展示字面量 `{fba_days}`，模板变量没替换 | 已知限制清单 |

广告数据的字段比补货多（花费、点击、曝光、订单、ACOS、TACOS…），
再用 HTML 传一遍，上面这些会成倍地再来一次。

**建议：** 上游脚本在生成任何页面之外，额外写一份
`data/ads/latest.json`（+ 按日期归档），OA 侧只读 JSON、自己渲染页面。
好处是 OA 完全掌控展示与脱敏，上游改版式不影响 OA，而且这份 JSON 顺手就能做单元测试。

## 决策 2：判定用规则，AI 只写"为什么和怎么调"

跟选品打分的分工完全一致：规则打分 + LLM 写理由（趋势发现那些
`trend_forecast` / `选品理由` / `行动建议` 就是这么来的）。

**规则负责判定**，因为判定必须可复现、可测试、可解释 —— 团队要能问
"为什么这条被标红"，答案得是一句话说得清的阈值，不是"模型觉得"。
而且它每天跑，成本和不确定性必须可控。

**LLM 负责解释和建议**，那是它擅长的：结合类目、竞争、季节说清
"这条为什么异常、先调哪个旋钮"。

### 建议的初始规则集

阈值一律**锚定 `config.json` 已有的值**，不另立标准（同 D10 的档位阈值做法）：

| 规则 | 判据 | 档位 |
|------|------|------|
| 花钱没出单 | 窗口内 `spend > 阈值` 且 `orders == 0` | 紧急 |
| ACOS 吃掉利润 | `acos > min_profit_margin`（config.json 现为 20%） | 紧急 |
| ACOS 突变 | `acos` 环比上升超过 N 个百分点 | 观察 |
| TACOS 拉升但销量没跟上 | `tacos` 上升且 `units` 持平/下降 | 观察 |
| 断量 | `impressions` 环比骤降超过 N% | 观察 |
| 预算撞顶 | 当日预算耗尽（若导出含此字段） | 观察 |

「紧急/观察/健康」三档直接复用补货已有的语义色和 `data-level` 约定，
不引入第四种状态词 —— 团队学一套就够。

## 决策 3：缺数据不补 0

沿用 D7 的 `CardData` 骨架（`{value, status, error}`）。
"这个 SKU 真的 0 花费"和"今天没导到数据"必须长得不一样 ——
广告尤其致命：把"没抓到"显示成"0 花费 0 单"，会让人以为广告停了去乱调。

## 决策 4：门户集成点

| 改什么 | 怎么改 |
|--------|--------|
| 新板块入口 | 只改 `oa/config.py` 的 `MODULES` 数组（D8 之后配置就在这一处） |
| 今日概览 | 加第五张卡「广告告警」：紧急 N / 观察 N / 在投 SKU N + Top 几条 |
| 页面 | 新增 `oa/ads.py`（读 JSON → `CardData`）+ 一个表格页，表格直接用 `.oa-table` 的现成密度和列权重 |
| 样式 | **不需要新增任何颜色** —— 语义色 `-ink`/`-soft` 三档已经够，见 D12 |

⚠️ 一个版式细节：顶部指标条现在是 6 格（`repeat(auto-fit, minmax(120px, 1fr))`），
再加 2 格在 1440px 下每格会挤到 ~150px。要么广告指标只上卡不上条，
要么指标条改成两行。这个等有真实数据再定。

---

## 需要你回答的四个问题

这四个问题的答案直接决定能做哪些规则、页面长什么样：

1. **领星导出的广告表有哪些字段？** 有 `spend`/`clicks`/`impressions`/`orders`/`sales` 就能跑全套规则；只有汇总 ACOS 的话规则要砍一半。给我一份表头就行。
2. **数据粒度是 SKU/ASIN 级，还是 campaign/关键词级？** SKU 级能直接跟补货页对齐（同一个 SKU 两个视角）；campaign 级更贴近实际调整动作，但要多一层聚合。
3. **更新频率？** 补货是周一/四，广告我建议至少日级 —— 广告的钱是每天在烧的，周级发现就晚了。
4. **公开页面怎么处理敏感字段？** 这条最需要你拍板 ——
   Pages 是全世界可见的。**广告花费和 ACOS 比毛利率更能反推出单位经济模型**，
   而 `PROJECT-VISION.md §6` 目前只把毛利率/月销量/库存列为敏感。
   两条路：
   - **走脱敏**（同 D10）：ACOS 和花费换成档位标签，只有紧急度和计数出数字
   - **换托管**：如果团队需要看真实数字，那广告板块就是「Pages 该不该继续公开」这个决定的触发点

---

## 不在本方案范围内

- 直连 Amazon Ads API（要走 LWA 授权，接入成本远超当前需求；领星导出已经够）
- 自动调价/自动改预算（OA 是决策工具，不是执行工具 —— 见 PROJECT-VISION §8）
- 广告投放效果归因分析（另一个量级的项目）
